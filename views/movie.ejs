<!DOCTYPE html>
<html>

<%- include('./partials/head') %>

    <body class="bg-body">
        <main class="grid-20-1fr gap-y-8 min-h-full w-full bg-primary absolute">
            <%- include('./partials/sidebar', { active: '/movies' }) %>
                <%- include('./partials/getMovies', { movies: [movie], hrefText: 'Boka' , href: '/booking/' , showIntro:
                    true, showReviews: true }) 
                    %>
        </main>
    </body>
    <template id="reviewTemplate">
        <li id="review__item" class="reviewItem flex flex-col justify-center h-20 px-2 py-2 text-left rounded-sm text-base bg-primary text-white">
        <p id="review__name" class="font-bold"></p>
        <p id="review__text"></p>
        <span id="review__rating" class="font-semibold"></span>
        </li>
    </template>
</html>